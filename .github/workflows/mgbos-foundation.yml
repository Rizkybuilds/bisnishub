name: MGBOS Foundation
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: mgbos-foundation-${{ github.ref }}
  cancel-in-progress: true
defaults:
  run:
    working-directory: mgbos
env:
  NEXT_TELEMETRY_DISABLED: "1"
jobs:
  application:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.34.5
      - uses: actions/setup-node@v4
        with:
          node-version-file: mgbos/.node-version
          cache: pnpm
          cache-dependency-path: mgbos/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - run: pnpm check
      - name: Production HTTP smoke tests
        shell: bash
        run: |
          pnpm --filter @mgbos/app start > /tmp/mgbos.log 2>&1 &
          mgbos_pid=$!
          pnpm --filter @mgbos/teestock start > /tmp/teestock.log 2>&1 &
          teestock_pid=$!
          trap 'kill "$mgbos_pid" "$teestock_pid" 2>/dev/null || true' EXIT
          for port in 3101 3102; do
            ready=false
            for attempt in $(seq 1 30); do
              if curl --fail --silent "http://127.0.0.1:$port/health" > /dev/null; then
                ready=true
                break
              fi
              sleep 1
            done
            if [ "$ready" != true ]; then
              cat /tmp/mgbos.log /tmp/teestock.log
              exit 1
            fi
          done
          pnpm test:integration
  database:
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.34.5
      - uses: actions/setup-node@v4
        with:
          node-version-file: mgbos/.node-version
          cache: pnpm
          cache-dependency-path: mgbos/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - run: pnpm db:start
      - run: pnpm db:reset
      - run: pnpm db:test
      - name: Generate database types reproducibly
        run: |
          pnpm db:types
          cp packages/database/generated/database.types.ts /tmp/database.types.ts
          pnpm db:types
          cmp /tmp/database.types.ts packages/database/generated/database.types.ts
      - uses: actions/upload-artifact@v4
        with:
          name: mgbos-database-types
          path: mgbos/packages/database/generated/database.types.ts
      - if: always()
        run: pnpm db:stop
